{
    "char_identity": {
        "type": "text",
        "description": "人物设定 (Visuals & Identity)",
        "hint": "【必填】请在此处填写角色的外貌描述。",
        "default": ""
    },
    "env_default": {
        "type": "text",
        "description": "环境 A: 卧室 (Prompt Block Only)",
        "hint": "只填写Prompt标签。默认值：\n(indoors, cute girl's bedroom aesthetic:1.3), (kawaii style:1.2), (natural window light mixed with warm indoor lamps:1.3)...",
        "default": ""
    },
    "env_fullbody": {
        "type": "text",
        "description": "环境 B: 更衣室 (Prompt Block Only)",
        "hint": "只填写Prompt标签。默认值：\n(indoors, pink aesthetic dressing room:1.4), (bright sunlight streaming through sheer curtains:1.4)...",
        "default": ""
    },
    "env_outdoor": {
        "type": "text",
        "description": "环境 C: 户外 (Instruction Only)",
        "hint": "只填写指令。默认值：\nRemove indoor description, use user specified scene, force add `(blurred background), (bokeh), (natural lighting)`.",
        "default": ""
    },
    "enable_safety_guard": {
        "type": "bool",
        "description": "启用安全与质量守卫 (Safety Guard)",
        "hint": "【开关】\n开启 -> 注入负面提示词 (Negative Prompt) 逻辑，有效防止人物出现多肢体、崩坏或生成不安全内容。\n关闭 -> 不强制注入负面提示。",
        "default": false
    },
    "negative_content": {
        "type": "text",
        "description": "负面提示词内容 (Negative Prompt)",
        "hint": "仅在上方开关开启时生效。默认包含防崩坏、防NSFW通用词。",
        "default": "(nsfw:2.0), (nude), (worst quality:1.4), (low quality:1.4), (bad anatomy), (bad hands), (text), (error), (missing fingers), (extra digit), (fewer digits), (cropped), (jpeg artifacts), (signature), (watermark), (username), (blurry), (extra limbs), (fused fingers), (too many fingers), (long neck), (mutated hands), (polar lowres)"
    },
    "enable_custom_camera": {
        "type": "bool",
        "description": "启用/注入摄影模式切换指令",
        "hint": "【开关】\n开启 -> 向系统注入“摄影模式切换”逻辑 (包含自拍/全身/默认)，并使用下方参数。\n关闭 -> 不注入任何摄影模式指令，完全由模型自由发挥构图。",
        "default": true
    },
    "cam_selfie": {
        "type": "text",
        "description": "镜头 A: 自拍 (需开启上方开关)",
        "hint": "只填写镜头参数。默认值：\n, (mirror selfie style:1.2), holding phone, looking at phone screen or mirror...",
        "default": ""
    },
    "cam_fullbody": {
        "type": "text",
        "description": "镜头 B: 全身 (需开启上方开关)",
        "hint": "只填写镜头参数。默认值：\n, full body shot, showing entire figure from head to toe, wide angle lens...",
        "default": ""
    }
}